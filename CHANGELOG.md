# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [1.5.6] - 2026-09-26

> 主题：**把"看得见"修复到底 + 内容提质**。回归 559 → **652**，题库 744 → **756**，知识点 246 → **249**。

### Fixed（看得见的问题）
- **资讯卡片文字被切**（反复三版才修对）：真凶是卡片自身的 `overflow: hidden` 把正文硬裁，
  表现为"字只剩上半截 / 后面的字看不见"。已移除该裁剪并改为流式布局（内容多高卡片就多高）。
  过程中还修掉两个同类陷阱：① CSS `-webkit-line-clamp` 在行高为小数（1.7×14=23.8px）时会
  **把末行从字形中间切开**；② 更后面的旧代规则 `.topic-card p, .news-card p { line-height:1.7 }`
  会覆盖行高设置（又是"两代规则、后者胜"的样式债）。
- **能力雷达轴标签被裁**：标签放在 R+17 处且带"名称+百分比"，会伸到 x≈293 而 viewBox 右边界只有 260。
  改为**百分比换到第二行** + viewBox 四边留白 + `overflow: visible` 兜底。
- **点顶部命令条时整页被压缩**：抽屉原先用 `margin-right: min(420px,92vw)` 挤压主区，改为**纯覆盖式**
  （页面布局完全不动，只从右侧滑入），并补齐「点击抽屉外关闭」。
- **窄窗下卡片内容显示不全**：5 处响应式网格轨道的最小值不会随容器缩小（如资讯 345px），
  溢出后被面板 `overflow: hidden` 裁掉 → 全部改为 `minmax(min(Npx,100%),1fr)`。
- **搜索框提示显示不全**：实际显示的是 i18n 字典里更长的版本；已缩短并在 `title` 保留完整说明，
  **中英两套同步**（改文案必须连 i18n 一起改）。
- **设置项填错要等到提问失败才暴露**：新增即时校验（地址必须以 http(s):// 开头、密钥含空格或过短），
  保存时格式有误则**拦下并保留原配置**，提示走 i18n 且切语言自动刷新。
- **后端把 15MB 整个打进安装包**（复查发现）：后端过滤只有一条 `.cache` 排除，`@types` / `.bin` /
  `*.map` / test / docs / `*.md` 全随包发布 → 逐项排除后**后端 15.0 → 8.0 MB（省 7MB）**。
  实测验证：运行时依赖 express/dockerode/ssh2/protobufjs 均在，并**实际启动打包后的后端**确认可用。

### Added
- **抽屉玻璃质感**：默认 40% 透明（可继续调），配 `backdrop-filter` 虚化与上沿高光；
  气泡也做半透明（否则"透"只体现在窄边上，等于没效果）。三个旋钮：`--cp-tint` / `--cp-bubble` / `--cp-blur`。
- **对比度检查补齐**：新增**焦点指示器**（WCAG 2.4.13/1.4.11 要求非文本 ≥3:1）、
  **半透明叠底**（气泡 ← 抽屉 ← 背后，两层都要达标）、以及新增组件的配色检查；
  同时修正"装饰性描边按正文阈值误判"的问题。检查器已接入 `npm test`（CI 生效）。
- **开启界面停留约 2s**：步骤间隔 140 → 400ms（5×400=2000ms），并加**保底补足**
  （初始化更快补足到 2s，初始化更慢不额外拖延）。
- **Toast 点主体任意处关闭**（原只能点 ✕），并在选中文字时不误关（保护复制）。
- **键盘可达收尾**：三个分类筛选列表（工具/靶场/演练）此前鼠标专用 → 全部可键盘操作；
  ↑↓ 导航推广到 8 个列表；模态焦点环绕补上断言。

### 内容
- **二进制领域去重提质**：发现 5 组重复（Fuzzing 竟有 3 份）—— 用"重定位为不同侧面"而非删除
  （id 不变 → 相关映射无悬空引用、条目数不掉）；另补 3 条缺的入门/初级知识点（读汇编 / gdb 调试 /
  ELF 与 PE 格式）+ 12 题。该领域初级档 1 → 3。

### 守护（新增 60+ 条断言，均验证过"注入违规会真的失败"）
层级顺序、动效令牌必须定义、不得 line-clamp、卡片不得 overflow:hidden、行高整数像素、
正文逐字完整、无省略号、网格轨道用 min()、打包白名单与后端过滤、i18n 双语文案、placeholder 长度预算……

## [1.5.5] - 2026-09-25

> 主题：**样式债清理 + 窄窗适配 + 键盘可达收尾**。回归 430 → **559**。

### Fixed
- **打包会把开发产物塞进安装包**（严重）：`extraResources` 用的是黑名单 `["**/*", "!sec-tutor.html"]`，
  于是测试脚本（`selftest.js`/`quiz_validate.js`/`build-single.js`/`verify-single.js`）、工具脚本（`*.py`）、
  设计预览页（`preview-1..5-*.html`）、`_bench.js`、`package.json` 全都会被打包，
  **且本地只要跑过一次 `npm install`，`node_modules`（15MB）也会一起进包**。
  已改为**白名单**：`index.html / app.js / styles.css / data.js / assets/**`
- **知识库搜索框残留旧代样式**：`.kb-search` 存在两代规则，旧代的 `margin-bottom: 16px` 无人覆盖 ——
  它如今是工具栏里的 flex 子项，同行兄弟都无此 margin，导致**搜索框在整行内偏高约 8px**（已整块删除旧代规则）
- **工具面板输出框与标签错位 8px**：v1.5.4 给代码块加复制按钮时包了一层容器，容器继承了旧元素的纵向 margin
  （已加 `.tb-row .code-wrap { margin: 0 }`）
- **三个分类筛选列表鼠标专用**：工具分类 / 靶场分类 / 演练分类都是 `<li>` + click，没有 `role`/`tabindex`/键盘
  → 键盘用户无法切换筛选。已统一改为可键盘操作（实测工具分类可键盘项 0 → 13）
- **代码块长行顶宽父容器**：补 `overflow-x: auto`（长代码行改为横向滚动）
- **超长词顶破卡片**：`Log4Shell（CVE-2021-44228）` 这类标题加 `overflow-wrap: anywhere`
- 构建产物 `sec-tutor.html` 被提交进版本库（1.2MB、每次构建都变）→ 已移出版本库并加入 `.gitignore`

### Added
- **窄窗适配（≤720px）**：知识库工具栏此前 5 个控件挤一行、搜索框被压到几乎不可用 →
  工具栏允许换行、**搜索框独占一行**、范围 chips 折行；顶栏副标题让位
- **列表键盘导航推广到 8 个容器**：领域筛选 / 知识点 / 靶场分类 / 演练分类 / 工具分类 /
  靶场列表 / 资讯列表 / 演练列表，统一按 `[data-clickable]` 取项（一套实现覆盖全部）
- **对比度检查补齐两类此前完全未覆盖的项目**：
  ① **焦点指示器**（WCAG 2.4.13/1.4.11 要求非文本 ≥3:1）——键盘用户唯一的位置线索，浅色 4.48–5.20、深色 9.21–10.29；
  ② v1.5.4/v1.5.5 新组件配色（搜索高亮 / 复制按钮 / 消息操作 / 空态 / 错误态），浅深两套主题 4.77–16.82。
  装饰性描边不再按正文阈值误判（改信息级输出）
- **失败提示都给出「下一步」**：口令保护启用/修改/关闭失败、PNG 导出失败（→ 可改用导出 PDF）、
  语音识别与图片处理失败（→ 可改用文字输入，不影响其余功能）
- 两处空态收敛到统一三态组件（导出对话无内容、复习无同类题），均带「下一步」

### 工程与守护（防回潮）
- **样式债审计脚本**：静态检测「同一选择器多代定义 + 后代重新布局 + 旧代盒模型声明未被覆盖」，
  把 800+ 条规则收敛到可人工判定的 4 条（真缺陷 2 处）
- **资源同步改为白名单过滤**（幂等、可 `--check`），两处资源目录现在只含运行时的 5 个文件
- 新增 7 类守护断言：旧代残留样式债、孤儿类选择器（死样式）、打包白名单、
  窄窗/长文本源码检查、中途切面板状态一致性、空态不得用裸写法、失败提示必含下一步
  —— **每条都验证过「注入违规会真的失败」**

## [1.5.4] - 2026-09-21

> 主题：**交互与用户体验**（不加内容，只修可用性、手感与效率）。回归断言 430 → **510**。

### Added
- **键盘可达（贯穿全应用）**：知识点卡片、靶场卡片、资讯卡片、领域项等 **56 个可点击元素**获得
  `role=button` + Tab 焦点 + Enter/Space 触发 + 无障碍名称 —— 此前纯键盘用户**打不开任何知识点**
- **「跳到主要内容」跳过链接**（Tab 第一站，聚焦时可见）
- **三态统一**：`stateBlock(kind, {title, hint, actionText})` 落实既有的 `.empty/.loading/.error` 约定
  （此前该约定写了却几乎没用），空态一律给出「下一步做什么」：清除搜索 / 看全部难度 / 去知识体系 / 去自测
- **搜索命中高亮**（`mark.hl`，命中词在结果卡片上标出）
- **代码块一键复制**：覆盖知识点详情、弹窗内容（题解/报告）与工具输出
- **智能问答消息级操作**：每条回答带「复制」「重新生成」（此前只有副驾驶有）
- **自测成绩页「去看薄弱知识点」**：按错题领域聚合薄弱排行，一键跳到对应领域
- **知识库键盘导航**：搜索框 ↓ 进结果、结果间 ↑↓ 移动、↑ 到顶或 Esc 回搜索框
- **知识点详情**：上一个 / 下一个 + 位置指示（N / M）、`M` 标记/取消掌握、`← →` 前后、`Esc` 返回列表
- **自测键盘化**：`↑↓` 选择选项、`Enter` 提交 / 下一题（不用数字键，避免与面板快捷键冲突）
- **首次使用引导**：三步讲清「从今日开始 / 键盘更快 / 学完就练」，设置里可重新查看
- **失败可重试**：靶场环境申请失败时，面板与提示条都给出「重试」

### Changed
- **快捷键顺序与侧栏视觉顺序一致**：此前最左的「今日」被编成 9、第二位「知识体系」才是 1，
  按 1 根本到不了第一个面板；`TAB_KEYS` 改为直接读 DOM，单一数据源
- **可撤销操作改为「直接执行 + 限时撤销」**：重置掌握进度、清空靶场战绩、标记掌握均提供撤销，
  移除 3 处原生 `confirm()` 弹窗
- 快捷键面板补全：知识库导航、详情页、自测、9 个面板

### Fixed
- **知识库搜索框「框里套框」**：`.kb-search` 存在两代 CSS 规则并存（UI 换血遗留），新一代只重置了
  `background/border/outline`，旧代的 `padding`/圆角/`box-shadow` 与 `:focus` 的 4px 焦点环继续生效
  → 聚焦时外层搜索框内部凭空多出一个「隐形小框」（亮色主题最明显），并顶掉输入文字的垂直居中。
  已显式归零并统一由外层 `.kb-search:focus-within` 提供焦点反馈
- **toast 队列死循环导致界面卡死**（严重）：`toast()` 的「最多 3 条」裁剪依赖 `closeToast` 成功移除节点，
  若宿主中存在未登记在 `toastRecs` 的残留节点则直接 return → 子节点数不变 → `while` 永不结束。
  已改为兜底直接移除 + 硬上限双保险
- **复制按钮污染代码文本**：按钮曾放在 `<pre>` 内，导致手动选中复制会带上「复制」二字；改为容器包裹
- **复制按钮在内容重写后消失**：幂等标记（`data-copy-ready`）在 `textContent` 重写后仍在，
  增强逻辑误判「已加过」而跳过；改为「标记 + 按钮实际存在」双重判断
- 详情页亮色主题下的焦点环缺失（`outline: none` 权重压过全局焦点规则）等 2 处可达性缺陷

## [1.5.3] - 2026-09-16

### Added
- 内容：知识点 211 → **246**，题库 639 → **744**，**12 个领域全部达到 20 个知识点**
  - 网络与内网安全：DHCP 与地址欺骗、堡垒机与运维通道、代理与出网管控、IPv6 双栈风险、零信任 ZTNA
  - 云原生与容器安全：CSPM 态势管理、云上漏洞优先级、IaC 安全、多租户隔离、云上灾备与业务连续性
  - 蓝队·安全运营：告警治理与降噪、值班与交接规范、证据保全与留痕、云上检测接入、事件沟通与对外口径
  - 移动安全：客户端数据加密、混淆与代码保护、应用商店与上架安全
  - 数据安全与隐私：数据资产地图与血缘、数据对外共享、AI 训练数据合规
  - 供应链安全：许可证合规实操、依赖升级工程化、引入组件安全审计
  - AI·LLM 安全：RAG 安全工程实践、Agent 护栏工程实现、AI 生成内容标识与合规
  - 威胁情报与 SOC：情报共享与交换、网络取证与流量回溯、狩猎数据工程
  - 二进制：静态分析与模式识别、内核漏洞与提权；密码学：随机数与熵源安全、侧信道与实现安全
  - 渗透测试：云环境渗透要点
- 可达性：新增「跳到主要内容」跳过链接（Tab 第一站，聚焦时可见）
- 可达性：i18n 支持 `data-i18n-aria`，读屏文案随中英切换

### Fixed
- 可达性：`.update-notes > summary` 与 `.copilot-input` 只写了 `outline:none` 且选择器权重高于全局
  `:focus-visible` → 键盘用户看不到焦点位置，已补可见焦点环
- 可达性：4 个控件只有 placeholder 或未关联的 `<label>`（知识搜索、问答输入、工具输入、工具操作下拉）→ 补无障碍名称

### Changed
- 回归断言 424 → **430**：新增可读名称（208 个可交互元素）、输入控件名称、`outline:none` 必须有焦点补充、
  跳过链接与落点、切语言后 aria-label 跟随与复原

## [1.5.2] - 2026-09-15

### Fixed
- **自动更新完全失效**：`electron-updater` 运行时要读 `resources/app-update.yml`，而 `--prepackaged`
  封包流程跳过了 electron-builder 写它的步骤 → 安装版与 Portable 副本都缺该文件，
  点「检查更新」直接抛 `ENOENT: no such file or directory, open '...resources\app-update.yml'`
  （该文件自 v1.2 起从未存在，即自动更新从未真正启用）。现已补入并确认打进安装包
- 更新器形态判定新增 `noconfig`：缺配置时不再抛 ENOENT，而是提示「配置缺失，请重新安装完整安装版」

### Note
- v1.5.1 及更早的**已安装**用户无法通过自动更新升级（旧版包内没有配置，无法自举），
  需手动覆盖安装一次本版；此后自动更新恢复正常

## [1.5.1] - 2026-09-13

### Added
- 内容大版本：知识点 74 → **211**，题库 198 → **639**，领域 7 → **12**
- 新增 5 个领域：移动安全、数据安全与隐私、供应链安全、AI·LLM 安全、威胁情报与 SOC
- 12 个领域各补入门知识点；入门档题目 51 → 148（成为四档最多）
- 每个领域平均每知识点 ≥3 题（新增回归断言守住）

### Fixed
- 自测领域下拉原先硬编码 4 个领域（新增领域选不到）→ 改为按数据动态生成
- 检索精度：内容扩容导致 P@1 由 73.3% 降至 46.7% → 索引给名称与关键词做字段加权后回升至 **73.3%**
- `pentest` 中 id 为 `cloud` 的知识点与云原生领域 id 重名 → 改名 `cloud-basics`

## [1.5.0] - 2026-09-11

> 本版合并了原计划分两次发布的 v1.3.0 / v1.4.0 全部改动（那两个版本号未单独发版）。

### Added
- 「今日」学习驾驶舱：四步主线推荐（到期复习 / 薄弱补强 / 顺势推进 / 自测），每步给出理由与真实阅读时长
- 能力可视化：7→12 领域能力雷达（带百分比与诊断基线）、14 天学习热力条、难度分布（可点击跳转筛选）
- 全局副驾驶：顶栏命令条（`Ctrl /` 唤起）+ 右侧抽屉，自动携带上下文、流式输出、可停止/复制、对话持久化
- 键盘可达性与响应式：侧栏语义化、焦点环统一、三档断点走查

### Changed
- UI 进化：新配色（青绿主色一脉到底）、排版层级重建、留白分两档、玻璃只给少数位置
- 对比度全部 ≥ AA；Orbitron 字体随包分发

## [1.2.4] - 2026-09-08

### Added
- 更新说明支持 markdown 渲染；启动自检与安全模式（异常时给出可恢复入口）

### Changed
- README 补首次安装 SmartScreen 绕过引导

## [1.2.3] - 2026-09-08

### Added
- 更新体验补完：下载进度与备份（更新前保留旧版本，可回退）

## [1.2.2] - 2026-09-03

### Added
- 崩溃兜底与落盘日志（异常可追溯）

## [1.2.1] - 2026-09-03

### Added
- 自动更新周期复查、失败重试与托盘进度

## [1.2.0] - 2026-09-03

### Added
- 自动更新（electron-updater + GitHub Releases）与版本化封包流程

### Fixed
- CI 单文件校验在 jsdom 环境缺 `TextEncoder` 导致 ReferenceError

## [1.1.0] - 2026-09-02

### Added
- Agent 增强：Flow 引擎、知识图谱依赖边、Agent 质量度量
- UI 打磨与响应式适配

### Changed
- `window.__agent` 改为惰性工厂（根除 TDZ 风险）

## [0.1.0] - 2026-08-24

### Added
- 前端单页应用（纯静态、离线可用）：
  - 7 大领域知识体系（Web / 二进制 / 密码学 / 渗透 / 网络 / 云安全 / 蓝队），含难度筛选与全局搜索
  - 随机自测：198 道题库，选项乱序 + 长度均衡（杜绝"选最长的就答对"）
  - 自适应能力诊断（答对升级、答错停该域），输出 7 域能力画像
  - 遗忘曲线复习（艾宾浩斯间隔）+ 弱项专项自测
  - 实战靶场：SQL 注入 / XSS / 路径遍历 / NoSQL / JWT 等在线演练，可对接后端一键生成 Docker 环境
  - 工具箱（Base64 / MD5 / SHA256）、智能问答（内置知识引擎 + 可选外部 LLM 流式输出）
- 后端服务（Express + dockerode）：靶场容器编排、配额、审计、CORS、异步回收
- Electron 桌面壳（可打包 NSIS 安装包）
- 测试体系：前端 jsdom 自测 145 项断言 + 题库结构校验 + 单文件构建校验；后端 smoke / quota / stress / Redis 降级 / 审计 / CORS / 契约测试
- CI：前端 GitHub Actions（测试 + 单文件构建）；后端镜像构建 + Trivy 漏洞扫描门禁
- 开源配套：MIT License、SECURITY.md、根 README

### Fixed
- 随机自测"正确答案总在同一位置"：选项乱序 + answer 下标重映射
- 随机自测"选最长的就一定正确"（97% 题正确项为最长选项）：运行时长度均衡，使长度与答案脱钩（正确率降至 ≈25%）
- 自测脚本 jsdom 依赖可移植化（不再绑定本机绝对路径）
