# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [1.5.4] - 2026-09-21

> 主题：**交互与用户体验**（不加内容，只修可用性、手感与效率）。回归断言 430 → **510**。

### Added
- **键盘可达（贯穿全应用）**：知识点卡片、靶场卡片、资讯卡片、领域项等 **56 个可点击元素**获得
  `role=button` + Tab 焦点 + Enter/Space 触发 + 无障碍名称 —— 此前纯键盘用户**打不开任何知识点**
- **「跳到主要内容」跳过链接**（Tab 第一站，聚焦时可见）
- **三态统一**：`stateBlock(kind, {title, hint, actionText})` 落实既有的 `.empty/.loading/.error` 约定
  （此前该约定写了却几乎没用），空态一律给出「下一步做什么」：清除搜索 / 看全部难度 / 去知识体系 / 去自测
- **搜索命中高亮**（`mark.hl`，命中词在结果卡片上标出）
- **代码块一键复制**：覆盖知识点详情、弹窗内容（题解/报告）与工具输出
- **智能问答消息级操作**：每条回答带「复制」「重新生成」（此前只有副驾驶有）
- **自测成绩页「去看薄弱知识点」**：按错题领域聚合薄弱排行，一键跳到对应领域
- **知识库键盘导航**：搜索框 ↓ 进结果、结果间 ↑↓ 移动、↑ 到顶或 Esc 回搜索框
- **知识点详情**：上一个 / 下一个 + 位置指示（N / M）、`M` 标记/取消掌握、`← →` 前后、`Esc` 返回列表
- **自测键盘化**：`↑↓` 选择选项、`Enter` 提交 / 下一题（不用数字键，避免与面板快捷键冲突）
- **首次使用引导**：三步讲清「从今日开始 / 键盘更快 / 学完就练」，设置里可重新查看
- **失败可重试**：靶场环境申请失败时，面板与提示条都给出「重试」

### Changed
- **快捷键顺序与侧栏视觉顺序一致**：此前最左的「今日」被编成 9、第二位「知识体系」才是 1，
  按 1 根本到不了第一个面板；`TAB_KEYS` 改为直接读 DOM，单一数据源
- **可撤销操作改为「直接执行 + 限时撤销」**：重置掌握进度、清空靶场战绩、标记掌握均提供撤销，
  移除 3 处原生 `confirm()` 弹窗
- 快捷键面板补全：知识库导航、详情页、自测、9 个面板

### Fixed
- **toast 队列死循环导致界面卡死**（严重）：`toast()` 的「最多 3 条」裁剪依赖 `closeToast` 成功移除节点，
  若宿主中存在未登记在 `toastRecs` 的残留节点则直接 return → 子节点数不变 → `while` 永不结束。
  已改为兜底直接移除 + 硬上限双保险
- **复制按钮污染代码文本**：按钮曾放在 `<pre>` 内，导致手动选中复制会带上「复制」二字；改为容器包裹
- **复制按钮在内容重写后消失**：幂等标记（`data-copy-ready`）在 `textContent` 重写后仍在，
  增强逻辑误判「已加过」而跳过；改为「标记 + 按钮实际存在」双重判断
- 详情页亮色主题下的焦点环缺失（`outline: none` 权重压过全局焦点规则）等 2 处可达性缺陷

## [1.5.3] - 2026-09-16

### Added
- 内容：知识点 211 → **246**，题库 639 → **744**，**12 个领域全部达到 20 个知识点**
  - 网络与内网安全：DHCP 与地址欺骗、堡垒机与运维通道、代理与出网管控、IPv6 双栈风险、零信任 ZTNA
  - 云原生与容器安全：CSPM 态势管理、云上漏洞优先级、IaC 安全、多租户隔离、云上灾备与业务连续性
  - 蓝队·安全运营：告警治理与降噪、值班与交接规范、证据保全与留痕、云上检测接入、事件沟通与对外口径
  - 移动安全：客户端数据加密、混淆与代码保护、应用商店与上架安全
  - 数据安全与隐私：数据资产地图与血缘、数据对外共享、AI 训练数据合规
  - 供应链安全：许可证合规实操、依赖升级工程化、引入组件安全审计
  - AI·LLM 安全：RAG 安全工程实践、Agent 护栏工程实现、AI 生成内容标识与合规
  - 威胁情报与 SOC：情报共享与交换、网络取证与流量回溯、狩猎数据工程
  - 二进制：静态分析与模式识别、内核漏洞与提权；密码学：随机数与熵源安全、侧信道与实现安全
  - 渗透测试：云环境渗透要点
- 可达性：新增「跳到主要内容」跳过链接（Tab 第一站，聚焦时可见）
- 可达性：i18n 支持 `data-i18n-aria`，读屏文案随中英切换

### Fixed
- 可达性：`.update-notes > summary` 与 `.copilot-input` 只写了 `outline:none` 且选择器权重高于全局
  `:focus-visible` → 键盘用户看不到焦点位置，已补可见焦点环
- 可达性：4 个控件只有 placeholder 或未关联的 `<label>`（知识搜索、问答输入、工具输入、工具操作下拉）→ 补无障碍名称

### Changed
- 回归断言 424 → **430**：新增可读名称（208 个可交互元素）、输入控件名称、`outline:none` 必须有焦点补充、
  跳过链接与落点、切语言后 aria-label 跟随与复原

## [1.5.2] - 2026-09-15

### Fixed
- **自动更新完全失效**：`electron-updater` 运行时要读 `resources/app-update.yml`，而 `--prepackaged`
  封包流程跳过了 electron-builder 写它的步骤 → 安装版与 Portable 副本都缺该文件，
  点「检查更新」直接抛 `ENOENT: no such file or directory, open '...resources\app-update.yml'`
  （该文件自 v1.2 起从未存在，即自动更新从未真正启用）。现已补入并确认打进安装包
- 更新器形态判定新增 `noconfig`：缺配置时不再抛 ENOENT，而是提示「配置缺失，请重新安装完整安装版」

### Note
- v1.5.1 及更早的**已安装**用户无法通过自动更新升级（旧版包内没有配置，无法自举），
  需手动覆盖安装一次本版；此后自动更新恢复正常

## [1.5.1] - 2026-09-13

### Added
- 内容大版本：知识点 74 → **211**，题库 198 → **639**，领域 7 → **12**
- 新增 5 个领域：移动安全、数据安全与隐私、供应链安全、AI·LLM 安全、威胁情报与 SOC
- 12 个领域各补入门知识点；入门档题目 51 → 148（成为四档最多）
- 每个领域平均每知识点 ≥3 题（新增回归断言守住）

### Fixed
- 自测领域下拉原先硬编码 4 个领域（新增领域选不到）→ 改为按数据动态生成
- 检索精度：内容扩容导致 P@1 由 73.3% 降至 46.7% → 索引给名称与关键词做字段加权后回升至 **73.3%**
- `pentest` 中 id 为 `cloud` 的知识点与云原生领域 id 重名 → 改名 `cloud-basics`

## [1.5.0] - 2026-09-11

> 本版合并了原计划分两次发布的 v1.3.0 / v1.4.0 全部改动（那两个版本号未单独发版）。

### Added
- 「今日」学习驾驶舱：四步主线推荐（到期复习 / 薄弱补强 / 顺势推进 / 自测），每步给出理由与真实阅读时长
- 能力可视化：7→12 领域能力雷达（带百分比与诊断基线）、14 天学习热力条、难度分布（可点击跳转筛选）
- 全局副驾驶：顶栏命令条（`Ctrl /` 唤起）+ 右侧抽屉，自动携带上下文、流式输出、可停止/复制、对话持久化
- 键盘可达性与响应式：侧栏语义化、焦点环统一、三档断点走查

### Changed
- UI 进化：新配色（青绿主色一脉到底）、排版层级重建、留白分两档、玻璃只给少数位置
- 对比度全部 ≥ AA；Orbitron 字体随包分发

## [1.2.4] - 2026-09-08

### Added
- 更新说明支持 markdown 渲染；启动自检与安全模式（异常时给出可恢复入口）

### Changed
- README 补首次安装 SmartScreen 绕过引导

## [1.2.3] - 2026-09-08

### Added
- 更新体验补完：下载进度与备份（更新前保留旧版本，可回退）

## [1.2.2] - 2026-09-03

### Added
- 崩溃兜底与落盘日志（异常可追溯）

## [1.2.1] - 2026-09-03

### Added
- 自动更新周期复查、失败重试与托盘进度

## [1.2.0] - 2026-09-03

### Added
- 自动更新（electron-updater + GitHub Releases）与版本化封包流程

### Fixed
- CI 单文件校验在 jsdom 环境缺 `TextEncoder` 导致 ReferenceError

## [1.1.0] - 2026-09-02

### Added
- Agent 增强：Flow 引擎、知识图谱依赖边、Agent 质量度量
- UI 打磨与响应式适配

### Changed
- `window.__agent` 改为惰性工厂（根除 TDZ 风险）

## [0.1.0] - 2026-08-24

### Added
- 前端单页应用（纯静态、离线可用）：
  - 7 大领域知识体系（Web / 二进制 / 密码学 / 渗透 / 网络 / 云安全 / 蓝队），含难度筛选与全局搜索
  - 随机自测：198 道题库，选项乱序 + 长度均衡（杜绝"选最长的就答对"）
  - 自适应能力诊断（答对升级、答错停该域），输出 7 域能力画像
  - 遗忘曲线复习（艾宾浩斯间隔）+ 弱项专项自测
  - 实战靶场：SQL 注入 / XSS / 路径遍历 / NoSQL / JWT 等在线演练，可对接后端一键生成 Docker 环境
  - 工具箱（Base64 / MD5 / SHA256）、智能问答（内置知识引擎 + 可选外部 LLM 流式输出）
- 后端服务（Express + dockerode）：靶场容器编排、配额、审计、CORS、异步回收
- Electron 桌面壳（可打包 NSIS 安装包）
- 测试体系：前端 jsdom 自测 145 项断言 + 题库结构校验 + 单文件构建校验；后端 smoke / quota / stress / Redis 降级 / 审计 / CORS / 契约测试
- CI：前端 GitHub Actions（测试 + 单文件构建）；后端镜像构建 + Trivy 漏洞扫描门禁
- 开源配套：MIT License、SECURITY.md、根 README

### Fixed
- 随机自测"正确答案总在同一位置"：选项乱序 + answer 下标重映射
- 随机自测"选最长的就一定正确"（97% 题正确项为最长选项）：运行时长度均衡，使长度与答案脱钩（正确率降至 ≈25%）
- 自测脚本 jsdom 依赖可移植化（不再绑定本机绝对路径）
