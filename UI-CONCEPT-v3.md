# SecTutor UI 创新方案 v3

> 2026-09-10 ｜ 前两轮复盘：v1.4.0 换皮失败（只感到颜色变化）、液玻取消（技术上可行但没解决真问题）。
> 这一轮不再从"视觉风格"入手，改从**产品结构**找创新点。
> 观点：**一个学习工具最有价值的创新，不是长得好不好看，而是让人知道"现在该干什么"和"我进步了多少"。**

---

## 1. 先说结论：创新点在哪里

我把现状摸了一遍，发现三处"明明有料却没用起来"的地方：

| 现状 | 问题 | 创新机会 |
| --- | --- | --- |
| **8 个面板并列**（知识/问答/靶场/计划/资讯/工具/自测/合规） | 用户要自己决定先点哪个、学什么，**没有主线** | 把首页变成**今日主线**：系统告诉我现在做什么 |
| 已经存了 `state.masteryDates`（含日期）、`state.profile`（能力画像）、`state.mastery`（掌握度） | 这些数据只被渲染成**小进度条 + 数字**，没有任何图表 | **能力可视化**：雷达图 + 学习热力条 + 强弱分布 |
| AI 已被做成"智能问答"面板，有 6 个角色、工具白名单、上下文引擎 | 想提问必须**切到那个面板**，离开当前任务 | **全局副驾驶**：常驻命令条 + 上下文感知，不打断当前任务 |

**一句话**：这个项目缺的不是更好看的皮肤，而是把已有能力**组织成一条路径**、把已有数据**变成看得见的成长**。

---

## 2. 三个创新方向

### 方向 A · 学习驾驶舱（把并列面板变成一条主线）★ 推荐

**用户感知**：打开软件不再面对 8 个图标发愣，而是看到一条明确的路径——
"今天：先复习 3 个薄弱点（约 6 分钟）→ 学 1 个新知识点（8 分钟）→ 做 5 题自测（5 分钟）"。

**形态**：首页改为「今日」页，核心是一条**纵向任务流**（编号节点 + 连线），每项带：
- 为什么推荐它（"SQL 注入：距上次复习 3 天，掌握度 78%"）
- 预计用时
- 一键进入（直接跳到对应知识点/题目，而不是跳到面板再找）

**数据来源**：`state.mastery` + `masteryDates` + 复习曲线（已实现）→ **不需要新后端**。

**为什么它比换皮有效**：它改变的是"这个软件在替我做决定"的感觉——这是工具与助手的分界线。

---

### 方向 B · 能力可视化（把已有数据变成产品价值）★ 推荐

**用户感知**：能一眼看到"我的网络安全能力长什么样"，以及"这周比上周强在哪"。

**三个具体组件**：
1. **能力雷达**（6 个领域：Web / 二进制 / 密码学 / 渗透 / 网络 / 云）——当前能力 vs 上月，两条多边形
2. **学习热力条**（最近 14 天，按 `masteryDates` 着色）——一眼看出坚持情况
3. **强弱分布条**：把掌握度分档（已掌握 / 熟悉 / 生疏 / 未学），点击直接筛出薄弱项

**为什么值得做**：这是**唯一能把"进步"变可见**的东西。学习工具最大的心理需求就是"我到底有没有变强"。
顺便，这类页面天然适合截图分享，是传播点。

---

### 方向 C · 全局副驾驶（AI 从"一个面板"升级为"基础设施"）

**用户感知**：在任何位置按 `⌘K` / `Ctrl+K` 唤起命令条，直接问；
它能自动带上你正在看的东西（"这个知识点" = 当前打开的知识点 / 刚答错的题 / 靶场输出），
回答**就地展开为侧边抽屉**，不把你从当前任务踢走。

**形态**：
- 顶部常驻一条命令条（视觉上的主角），带上下文 chip，可删除/添加
- 回答面板从右侧滑入，宽 380px，可一边看知识点一边问
- 保留原「智能问答」面板作为深度对话模式

**数据来源**：已有 `askAgent` + `AGENT_ROLES` + `state` 上下文 → **不需要新后端**。

**为什么它现代**：这是 Cursor / Copilot / Notion AI 已经验证过的交互范式，
用户一看就知道"这软件是新的"。

---

## 3. 优先级与理由

| 顺序 | 做什么 | 为什么这个顺序 |
| --- | --- | --- |
| 1 | **A + B 一起做** | 它们是同一枚硬币：主线的推荐理由来自能力数据；先把"看得见的成长"和"明确的下一步"立起来 |
| 2 | **C 全局副驾驶** | 交互范式升级，改动集中在 index.html 顶部 + 一层抽屉；依赖 A/B 的上下文 hook |
| 3 | 面板级视觉统一（P1 排版 + P2 留白，已有试点） | 收尾，让新首页与旧面板风格一致 |

**为什么 A+B 优先于继续调视觉**：你上一轮说"只感觉到颜色变化"——
根因是**结构没变**。A 改的是信息架构，B 改的是"数据有没有变成价值"，两者都不依赖颜色。

---

## 4. 改动量与风险

| 方向 | 改动范围 | 是否需要后端 | 风险 |
| --- | --- | --- | --- |
| A 驾驶舱 | 新增「今日」页（index.html + 一个新的 render 函数）+ 现有面板保留 | 否 | 中：需要定"推荐算法"的口径（可先用简单规则：逾期复习 > 薄弱领域 > 未学基础） |
| B 能力可视化 | 新增 3 个可视化组件（纯 SVG/CSS，无依赖） | 否 | 低：数据都在，纯渲染 |
| C 全局副驾驶 | 顶部命令条 + 右侧抽屉 + 上下文收集 | 否 | 中：要与现有模态/焦点管理协调（已有 FOCUSABLE_SEL 与 Esc 处理可复用） |

**共同风险**：新首页会让"老用户找不到原来的入口"。应对：主线底部保留一个"全部功能"入口，
或 rail 图标不动（本来就在）。

---

## 5. 本轮明确不做

- **不再调色板、不再研究玻璃**（前两轮的教训：这些不改变结构）
- **不动后端的容器/靶场链路**（那是功能，不是 UI 创新）
- **不引框架、不引图表库**：雷达/热力条用手写 SVG + CSS 即可，避免新依赖与构建链

---

## 6. 附：可视原型

`cybersec-agent/preview-cockpit.html` —— 把 A + B 的核心界面先画出来（静态原型，加载真实样式表）。
看的是**结构**：顶部命令条（副驾驶）、左侧今日主线（编号节点 + 连线）、中间能力雷达 + 学习热力条、
右侧状态栏、底部今日小结。颜色沿用现有青绿一脉，重点不在配色。
