# SecTutor 视觉设计定稿 v2（U2a）

> 2026-09-09 ｜ 方向：**轻科幻玻璃风** ｜ 主色：**青绿 #0d9488 系** ｜ 标题字体：**Orbitron（打包 latin 子集，11.8KB）**
> 用途：这是 U2b 全量换血的施工依据。里面每个颜色都带**实测对比度**，不是凭感觉挑的。

---

## 1. 设计语言三条原则

**① 玻璃是容器的属性，不是内容的属性。**
半透明加模糊只给「面板容器 / 卡片 / 弹层 / 顶栏」这类**承载者**，长正文、代码块、密集列表一律用实底色。这是玻璃风最容易翻车的地方——把模糊铺在阅读区上，字会发虚，滚动还会掉帧。

**② 一个主色贯穿全部「可交互」语义。**
青绿同时等于按钮、链接、选中、焦点环、进度。**其它颜色只准当状态灯**（成功 / 警告 / 危险），不准抢主色的位置。上一版双霓虹（洋红＋青）最乱的地方就是「两个颜色都在说点我」。

**③ 科幻感来自结构和排版，不来自发光堆叠。**
细描边（1px）+ 顶部一道内阴影高光（模拟玻璃边缘）+ Orbitron 的英文标题与数字；发光全部收敛成「悬停或选中时才出现」，不做常态外发光。

---

## 2. 色板（附实测对比度）

全部比率用 WCAG 公式实算。正文需 ≥ 4.5，控件边界与焦点环等非文本需 ≥ 3.0。

### 浅色主题

| 令牌 | 值 | 用途 | 实测 |
| --- | --- | --- | --- |
| `--bg` | `#eaeff0` | 页面底（冷灰绿） | — |
| `--panel` | `rgba(255,255,255,.72)` + 模糊 | 玻璃卡片容器，观感有效色 `#f9fbfb` | — |
| `--panel-2` | `#ffffff` | 实底卡片 / 代码块 / 列表项 | — |
| `--ink` | `#10201f` | 正文 | **14.50** ✅ |
| `--muted` | `#5b6f6c` | 次要说明 | bg **4.60** / panel **5.14** / 白 **5.34** ✅ |
| `--line` | `rgba(16,32,31,.10)` | 装饰性分隔线 | 装饰用，不计 |
| `--border` | `#7f8f8c` | 输入框 / 控件边界 | **3.26** ✅（控件标准 ≥3.0） |
| `--brand` | `#0b7a71` | 主按钮填充，配白字 | 白字 **5.20** ✅ |
| `--brand-hi` | `#0d9488` | 选中态勾 / 边框强调 | **3.23** ✅（控件标准 ≥3.0） |
| `--link` | `#0e7490` | 链接 / 可点文字 | bg **4.62** / panel **5.16** ✅ |
| `--focus` | `#0b7a71` | 焦点环 | panel **5.01** ✅ |
| `--ok` / `--warn` / `--bad` | `#15803d` / `#a4530a` / `#c2262a` | 状态色（配白字） | **5.02** / **5.50** / **5.83** ✅ |

> 修正记录：候选 `#0d9488` 做按钮填充时白字只有 **3.74**（不达标），故按钮填充加深到 `#0b7a71`，`#0d9488` 降级只做选中边框。

### 深色主题

| 令牌 | 值 | 用途 | 实测 |
| --- | --- | --- | --- |
| `--bg` | `#071110` | 页面底（深墨绿） | — |
| `--panel` | `rgba(19,35,32,.72)` + 模糊 | 玻璃卡片容器，有效色 `#101e1c` | — |
| `--panel-2` | `#0d1a19` | 实底卡片 / 代码块 | — |
| `--ink` | `#dcedea` | 正文 | **15.82** ✅ |
| `--muted` | `#8ea8a4` | 次要说明 | bg **7.56** / panel **6.77** ✅ |
| `--line` | `rgba(255,255,255,.09)` | 装饰性分隔线 | 装饰用，不计 |
| `--border` | `#527a71` | 控件边界 | **3.58** ✅ |
| `--brand` | `#2dd4bf` | 主按钮填充，配**深墨字** `#04120f` | **10.27** ✅ |
| `--brand-2` | `#14b8a6` | 按钮渐变另一端 | 深字 **7.68** ✅ |
| `--link` | `#5eead4` | 链接 / 可点文字 | bg **12.94** / panel **11.59** ✅ |
| `--accent` | `#7dd3fc` | 次要强调（图表 / 标签） | panel **10.29** ✅ |
| `--focus` | `#2dd4bf` | 焦点环 | panel **9.21** ✅ |
| `--ok` / `--warn` / `--bad` | `#4ade80` / `#fbbf24` / `#f87171` | 状态色 | **9.84** / **10.27** / **6.20** ✅ |

> 关键决策：深色主题按钮走**亮青底加深墨字**（不是白字）。白字在 `#169d90` 上只有 3.35 不达标，反过来深字在亮青上有 10.27——顺带这也是“轻科幻”现代感的来源。

---

## 3. 字体层级

**打包 Orbitron latin 子集**（已放到 `cybersec-agent/assets/fonts/orbitron-latin.woff2`，11.8KB，`wOF2` 已验）。

```css
@font-face {
  font-family: "Orbitron";
  src: url("assets/fonts/orbitron-latin.woff2") format("woff2");
  font-weight: 400 900;   /* 变体字重，一个文件够用 */
  font-display: swap;
}
```

**使用白名单**（窄，因为 Orbitron 没有中文字形，长文本也难读）

- ✅ 面板标题里的英文与数字（`Web Security`、`OWASP Top 10`）
- ✅ 统计数字、百分比、徽章计数（`78%`、`12/50`）
- ✅ logo 与主标题 `SecTutor`
- ❌ 正文段落、代码块、表格、按钮文字（数字徽标除外）
- ❌ 任何中文标题——会 fallback 到系统栈，视觉分级改由**字重 / 字距 / 字号**承担，不要硬凑

**字号与行高**（替代现在散落的魔法数）

| 层级 | 字号 | 行高 | 用在哪 |
| --- | --- | --- | --- |
| display | 26 / 22px | 1.25 | 面板视觉主标题、大数字 |
| h2 | 18px | 1.35 | 面板标题 |
| h3 | 15.5px | 1.35 | 区块小标题（可用 Orbitron） |
| body | 15px | 1.62 | 正文（沿用现值，不动） |
| sm | 13px | 1.5 | 次要说明 |
| xs | 12px | 1.45 | 徽章、脚注 |
| micro | 11px | 1.4 | 计数、时间戳 |

---

## 4. 形状与密度

| 项 | 规格 |
| --- | --- |
| 圆角三档 | `--r-sm: 10px`（控件 / 徽章）｜`--r-md: 14px`（卡片）｜`--r-lg: 20px`（面板 / 弹层）——比现值大一档，玻璃边才显柔 |
| 玻璃规格 | `background: var(--glass)` + `backdrop-filter: blur(14px) saturate(125%)` + 1px 描边 + `inset 0 1px 0 rgba(255,255,255,.35)`（浅）/ `rgba(255,255,255,.06)`（深） |
| 降级 | `@supports not (backdrop-filter: blur(2px))` 时把 `--panel` 换成不透明近似色，老环境仍能正常看 |
| 阴影三档 | `--shadow-sm/md/lg`：低扩散、不加外发光，只做层次 |
| 控件高度 | 按钮 36（小）/ 40（默认）；输入框与 select 40；chip 30 |
| 栅格与间距 | 沿用 U1 的 `--sp-*` 4px 栅格；卡片内边距 16–18px，区块间距 14–16px |

**玻璃禁用区**（写进施工检查清单）：长正文段落、代码块与终端输出、密集列表项（知识点列表 / 搜索结果 / 报告表格）、聊天消息气泡。

**限制并发数量**：模糊有合成开销，同一屏只给顶栏、侧栏、主卡片容器这几个**静态**容器开玻璃；滚动区域内的卡片用实底，避免滚动时重绘。

---

## 5. 动效基调

现有 **16 个 @keyframes** 三清单（U2b 已按此执行完毕）：

- **保留（11 个，去掉就丢信息）**：`blink`（输入光标）、`fadeUp`（消息与卡片入场）、`modalIn` / `modalOut`、`panelIn`（切牌）、`popIn`、`skSweep`（骨架屏扫光）、`spin`（loading）、`toastIn` / `toastOut`、`typingBounce`（打字指示）
- **合并（5 个 → 3 个参数化定义）**：`pulse`、`cdPulse`、`flowPulse`、`recpulse`、`spPulse` 全在做「呼吸」，按语义归三组，每组只留一份定义，差异用自定义属性传：
  - `breath-op`（透明度呼吸，`--op-min` 控幅度）← 合并 `pulse` + `cdPulse`
  - `breath-ring`（环形扩散，`--ring-c` / `--ring-spread` 控颜色与距离）← 合并 `flowPulse` + `recpulse`
  - `breath-scale`（缩放呼吸，`--sc` 控幅度）← 原 `spPulse`
- **处理后总数**：16 → **14**，且同一语义的动效只有一处定义（比原计划的 12 多 2 个，因为实测下来「透明度 / 环形 / 缩放」三种呼吸无法共用一个 keyframes，硬并会让录音指示器等场景失真）

**时长与缓动**

- `--dur-fast: 140ms`（悬停 / 按下）｜`--dur-base: 220ms`（入场 / 位移）｜`--dur-slow: 320ms`（弹层 / 大位移）
- `--ease-out: cubic-bezier(.16,.84,.44,1)` 统一默认，清掉裸露的 `ease` / `linear` 写法不一致
- `--ease-spring`（过冲）**限用 ≤2 处**，只给「通关 / 答对」这类一次性正反馈；多了就是廉价感

**降级**：沿用现有结构，只有动画属性包在 `prefers-reduced-motion: no-preference` 里。

---

## 6. U2b 施工路径（换血顺序）

1. 替换第 1 段两套主题的**变量值**（第 7 段 utilities 全用令牌，自动跟随，无需改动）
2. 新增 `--glass` / `--blur` / `--r-*` / `--fs-*`，并在容器类上启用玻璃（严格按第 4 章禁用区）
3. 字体上线：加 `@font-face`，把白名单处的标题字体族改为 Orbitron 起头
4. 逐组件过一遍：按钮 / 输入 / chip / modal / 卡片 / toast / 进度条，清除游离于令牌外的硬编码值
5. `@keyframes` 按第 5 章三清单执行，同步清理调用点
6. 面板头部统一 + 8 个面板逐个走查换肤

**落地微调（U2b 实做时定的）**

- `--panel`（卡片等阅读区）不透明度从 .72 提到 **.88 / .86**：玻璃观感交给 `--glass` + blur 承担，
  阅读区保持接近实底，避免文字压在渐变上发虚。抬高后实测：浅色正文 16.51、次要 5.24；深色正文 14.24、次要 6.80。

**验收**

- 脚本重算全文对比度，全部达标（第 2 章的表就是 `cybersec-agent/contrast-check.py` 的输出，U2b 换完色再跑一次比对）
- 双主题各走一遍 8 个面板；装了 / 没装 Orbitron 的机器视觉一致（因为字体已随包）
- 回归 367 断言全绿 + 无新增异常

---

## 7. 风险与待确认

| 风险 | 应对 |
| --- | --- |
| Orbitron 无中文字形，中文标题必然 fallback | 分级改由字重 / 字距 / 字号承担，已在第 3 章写明，不追求字体统一 |
| `backdrop-filter` 性能 | 限制并发数量 + 滚动区域禁用（第 4 章） |
| 老 Electron / WebView 不支持模糊 | `@supports` 降级到不透明背景 |
| 玻璃 + 深色混合时让重点不突出 | 状态色只用在真正需要警示的地方，主色保持在青绿一脉 |
| 字体 License | Orbitron 为 OFL，随包分发需在项目 LICENSE 中注明（U2b 时补） |
