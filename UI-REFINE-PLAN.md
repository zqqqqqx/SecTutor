# SecTutor UI 优化方案 v2（针对「只感觉到颜色变化」）

> 2026-09-10 ｜ v1.4.0 换血后的复盘与下一步
> 结论先行：**问题不在颜色，在于这次改的全是"值"，没改"结构"。** 下面每条诊断都带实测数据。

---

## 1. 诊断：为什么只感觉到颜色变化

| # | 根因 | 实测证据 | 说明 |
| --- | --- | --- | --- |
| 1 | **排版层级塌陷** | 字号 84 处挤在 12–13.5px；20px 仅 3 处、15px 仅 4 处。标题/正文字号比仅 **1.15–1.33** | 人眼判断"设计感"第一靠字号对比。标题和正文差不多大，整个界面就是平的，换什么颜色都平 |
| 2 | **留白过密** | `gap` 以 6/8/10px 为主（49 处），`padding` 以 2/6/10px 为主 | 现代工具感来自"呼吸感"。卡片内边距 14px、栅格间距 12px 都属于紧凑档，看着就像"没改" |
| 3 | **表面层次扁平** | 面板与卡片亮度差仅 **1.96%**（理想 3–6%） | 卡片和背景几乎同色，阴影又弱，卡片"浮不起来"，视觉上等于没有卡片 |
| 4 | **玻璃实际上看不见** | v1.4.0 把 `--panel` 提到 .88 不透明度、玻璃只给静态容器 | 当初为了文字清晰把玻璃调没了，结果最有个性的那笔等于没画 |
| 5 | 字体改动对中文无效 | Orbitron 只覆盖英文与数字，中文全部回落系统栈 | 界面 90% 是中文 → 字体这一项对观感贡献接近于零（这个是预期内的，但意味着必须靠字号/字重补） |
| 6 | 图标不统一 | stroke-width 有 1.7 / 1.8 / 1.9 / 2 四种 | 细节噪声，单独看不出来，累积起来显得"没收拾干净" |

**一句话总结**：v1.4.0 换了色板、换了圆角（12→14）、换了字体（对中文无效）——但**没动层级、没动留白、没动层次**，而这三项才是"看得出换了皮"的来源。

---

## 2. 感知杠杆排序（先做上面，效果最明显）

| 杠杆 | 感知强度 | 改动成本 | 风险 |
| --- | --- | --- | --- |
| ① 字号与字重层级 | ★★★★★ | 中 | 中（可能挤版，需逐面板看） |
| ② 留白与间距节奏 | ★★★★★ | 中 | 中（信息密度下降） |
| ③ 表面分层（背景/面板/卡片/浮层四级亮度差） | ★★★★☆ | 低 | 低 |
| ④ 卡片投影与 hover 抬升 | ★★★★☆ | 低 | 低 |
| ⑤ 数据可视化（环形进度 + 大字数值） | ★★★★☆ | 中 | 低（局部） |
| ⑥ 让玻璃真正可见（集中 2–3 处 + 背景有东西可糊） | ★★★☆☆ | 中 | 中（文字清晰度） |
| ⑦ 入场动效错峰 | ★★★☆☆ | 低 | 低（reduced-motion 需降级） |
| ⑧ 图标统一（stroke 1.75 + 24 网格） | ★★☆☆☆ | 低 | 低 |
| ⑨ 再调颜色 | ★☆☆☆☆ | 低 | —（已到收益递减区，本轮**不建议**再动色板） |

---

## 3. 优化方案（分四批，每批独立可验收）

### P1 · 排版层级重建（感知最强，优先做）

**现状 → 目标**

| 角色 | 现状 | 目标 | 变化 |
| --- | --- | --- | --- |
| 面板大标题 / 大数字 | 20–22px（仅 3 处） | **28 / 24px**，字重 700，行高 1.25 | 拉开头部 |
| 区块标题 h2 | 16–18px | **21px**，字重 650–700，行高 1.3 | 明确二级 |
| 小标题 h3 | 12–12.5px（现被当正文用） | **16.5px**，字重 600 | 从"正文大小"提升为"标题" |
| 正文 | 13–13.5px（实际主流值） | **15px**，行高 1.7 | 正文反而变大，可读性提升 |
| 次要说明 | 12px | **13px**，行高 1.55 | — |
| 徽章 / 脚注 | 11px | **12px** | — |
| 数字 / 英文 | 系统字体 | Orbitron + `tabular-nums` | 数据跳出来 |

**关键指标**：标题/正文字号比从 **1.15–1.33 → ≥1.4**（大标题 28 / 正文 15 ≈ 1.87）。

**配套**：标题上方留白 > 下方留白（如 24 / 10），形成"标题属于它下面内容"的视觉分组——这是最便宜的高级感来源。

**验收**：知识库、测验两个面板前后对照样张；逐面板检查无换行溢出、无按钮错位；回归 367 全绿。

---

### P2 · 留白与密度重排

**现状 → 目标**

| 项 | 现状 | 目标 |
| --- | --- | --- |
| 卡片内边距 | 14px | **20px**（大卡片 24px） |
| 栅格间距 | 12px | **16–18px** |
| 面板内边距 | 14–16px | **24px** |
| 列表行高 | 34px | **40–42px** |
| 区块与区块间距 | 14–16px | **24px** |
| 控件间距 | 6–8px | **10–12px** |

**取舍提示（需要你拍板）**：留白变大 → 单屏信息量下降约 15–20%。
- 方案 A（推荐）：知识/测验/靶场等**阅读型**面板放大留白；聊天、终端等**交互型**保持紧凑。
- 方案 B：全部统一放大（更一致，但聊天区会显得空）。

**验收**：同一屏下对比"信息条数"与"呼吸感"，取平衡点；8 面板逐个走查。

---

### P3 · 表面分层与「看得见的玻璃」

**四级表面亮度梯度**（浅色，目标差 3–6%）

| 层 | 现在 | 目标 | 用途 |
| --- | --- | --- | --- |
| L0 应用背景 | 85.5 | **85.5** | 页面底 |
| L1 面板 | 98.0 | **92** | 面板容器 |
| L2 卡片 | 100 | **97** | 卡片、列表块 |
| L3 浮层 | 100 | **100 + 强阴影** | 弹层、Toast、下拉 |

（深色同理，按亮度做 4 档，差值保持在 3–6%）

**卡片**：默认无描边，靠背景差 + `--shadow-sm` 分层；hover 时 `translateY(-2px)` + 阴影升到 `--shadow-md`，过渡 140ms。

**玻璃改法（关键）**：
- 玻璃**只保留 2–3 处**——顶栏、弹层、（可选）侧栏。把预算集中在少数位置，效果比到处撒强得多。
- 让背景**有东西可糊**：把 `body::before` 的两处极光斑加大、不透明度 0.10 → **0.16–0.18**，色相拉开（青绿 + 天蓝），这样玻璃后面才有内容。
- `--panel` 回到 **.78–.80**，但**只在玻璃容器上用**；卡片仍走接近实底的 L2，保证文字清晰。

**验收**：玻璃处能明显看到背景被模糊；卡片与面板亮度差实测 3–6%；文字对比度重跑 `contrast-check.py` 全部 ≥ AA。

---

### P4 · 签名元素、数据可视化与动效

1. **卡片左侧 3px 领域色条**（已有 `.topic-card::before`，推广到资讯卡、工具卡）+ 顶部一道渐变细线 → 形成统一记忆点。
2. **数据可视化**：掌握度环形、能力画像、学习路径进度，从"普通进度条"改成**环形 + Orbitron 大字数值**（`78%` 用 24–28px），这是"专业工具感"最强的一笔。
3. **入场动效错峰**：面板内卡片依次入场，间隔 40ms（`animation-delay: calc(var(--i) * 40ms)`），总时长控制在 400ms 内；`prefers-reduced-motion` 下整体关闭。
4. **图标统一**：全部 stroke-width **1.75**、24×24 网格、圆角端点、统一 2px 内边距。
5. **状态反馈**：按钮 loading 态（内联 spinner + disabled）、列表选中态左侧色条、Toast 位置与动画统一。

**验收**：数据区截图对比；动效在 60fps 下无明显掉帧；reduced-motion 开启后无动画。

---

## 4. 执行顺序建议

```
P1 排版层级  →  P2 留白密度  →  【出前后对照样张，确认方向】  →  P3 表面分层与玻璃  →  P4 签名元素与动效
   (感知最强)      (同样强)              (中途确认点)              (低风险见效)          (收尾加分)
```

**建议先做 P1+P2 就出样张对比**——这两项做完，观感变化会是"换了个软件"级别的；如果这时你觉得信息变太松，P2 可以往回收一档，成本很低。

**试点策略**：先在**知识库**（阅读型）和**测验**（交互型）两个面板上改，前后对照确认后，再推广到其余 6 个面板。这样即使方向要调，返工范围也只有 1/4。

---

## 5. 验收方式要改（这次的教训）

v1.4.0 的验收是"对比度全达标 + 自测全绿 + 无未定义令牌"——**这些都是正确性指标，不是感知指标**。
所以才会出现"技术上全对，但看起来只换了颜色"。

新增加三条感知验收：

1. **前后对照样张**：每个改动项都要有「改前 / 改后」并排样张（沿用 `preview-u2-real.html` 的做法，直接加载真实 styles.css）。
2. **量化指标**：标题/正文字号比 ≥1.4、表面亮度差 3–6%、卡片内边距 ≥20px、栅格间距 ≥16px。
3. **一句人话验收**：随机找一个改动后的面板，3 秒内能不能说出"哪里不一样了"——说不出来就说明改得不够。

---

## 6. 风险与取舍

| 风险 | 应对 |
| --- | --- |
| 字号变大导致按钮/标签换行错版 | P1 后逐面板走查；准备 `--fs-*` 令牌，一处回退全部回退 |
| 留白变大，单屏信息量下降 | P2 分阅读型/交互型两档；先试点再全量 |
| 玻璃降低不透明度后文字发虚 | 玻璃只给 2–3 处，卡片保持接近实底；对比度脚本每次必跑 |
| 动效错峰在低端机掉帧 | 限制卡片数量 ≤ 12 个参与错峰；reduced-motion 整体关闭 |
| 改到一半发现方向不对 | 试点只改 2 个面板，返工范围可控 |

---

## 7. 决策与试点执行记录（2026-09-10）

**用户拍板**
| 项 | 选择 |
| --- | --- |
| 执行方式 | **先试点知识库 + 测验两个面板**，出前后对照确认后再推广到其余 6 个 |
| 留白力度 | **分面板两档**：阅读型（知识/靶场/资讯）放大，交互型（聊天/终端）保持紧凑 |
| 玻璃 | **集中 2–3 处并重做背景**：只留图标栏 / 顶栏 / 弹层 / 通知，侧栏改实底 |

**已全量推广到 6 个面板（2026-09-10）**：把原来按面板 ID（#panel-knowledge / #panel-quiz）写的规则
改写成**按组件类**，于是其余面板自动跟随 —— 阅读型（靶场 / 资讯 / 学习计划 / 合规）一起放大字号与留白，
交互型（聊天 / 工具）保持紧凑。具体：卡片标题 15.5–16 → 16.5px；卡片正文 13–13.5 → 14px；详情正文 14.5px；
卡片内边距 19 → 20px；`.week-block` 9 → 12px、`.compliance-box` 28 → 30px；侧栏 16px（仅知识/测验/靶场三处）。

**试点落地时的原始记录（保留）**

| 批 | 落地内容 |
| --- | --- |
| P1 排版 | 知识库：hero 标题 20→**24px/700**、卡片标题 16.5→**17px**、卡片正文 13.5→**14px/1.7**、掌握度数字用 Orbitron **16px**；测验：题目 16→**21px/650**、选项 **15px** 且内边距 13/16、进度文字 13.5px。标题上方留白改 22px（大于下方 8px） |
| P2 留白 | 知识库：卡片内边距 16→**20px**、栅格 16→**18px**、侧栏 12→**16px**、领域项 10/12px；测验：卡片 20→**22px**、栏间距 18px（交互型只微调） |
| P3 分层 | 新增四级表面令牌：浅色 `#f1f5f5 / #f8fbfa / #ffffff`（亮度差 5.1 / 5.3 / 4.2%），深色 `#101f1d / #162a26 / #1d352f`（对比度链 1.13 / 1.13 / 1.15）。面板与卡片改走实色分层，**去掉原来"面板到卡片用渐变"的糊法** |
| P3 玻璃 | 背景极光斑加浓（0.10 → 0.17，深色 0.20）并取消 `body::before` 的 .95 透明度；玻璃收敛到图标栏 / 顶栏 / 弹层 / 通知，侧栏改实底 |
| P4 签名 | 卡片顶部 2px 领域色渐变细线（用 `background-image` 实现，避开 `.topic-card::after` 已被"已掌握"勾占用的问题）+ hover 抬升 2px；卡片入场错峰 40ms 递增（`prefers-reduced-motion` 下整体关闭）；图标线宽统一 1.75 |

**对照方式**：`preview-compare.html`（上下两帧，各含浅色与深色），改前用 `preview-before.css` 快照渲染。
——这正是 v1.4.0 缺的那一环：当时只有"技术正确性"验收，没有"感知对照"验收。

**指标对比**

| 指标 | 改前 | 改后 |
| --- | --- | --- |
| 知识库 标题/正文字号比 | 1.48（20 / 13.5） | **1.71（24 / 14）** |
| 测验 题目/选项字号比 | 1.14（16 / 14） | **1.40（21 / 15）** |
| 卡片内边距 | 16px | **20px** |
| 面板→卡片亮度差（浅色） | 1.96% | **5.25%** |
| 玻璃可见性 | 几乎不可见（--panel .88 无模糊） | 顶栏/弹层真模糊 + 背景有极光可糊 |

---

## 8. 本轮明确不做

- **不再动色板**：青绿主色已经定了，再调是收益递减。
- **不引 UI 框架 / 构建链**（沿用既有约束，浏览器直开必须继续可用）。
- **不做大规模 HTML 结构重构**：本轮只在样式层与"加 class"层面改，不动 DOM 结构（弹窗模板化仍然搁置）。
