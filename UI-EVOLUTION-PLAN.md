# SecTutor UI 进化方案（v2 · 决策已锁定）

> 2026-09-09 ｜ 本轮只出方案不动代码 ｜ 摸底数据来自当前 styles.css / app.js / index.html 实测

## 0.1 U1 执行结果（2026-09-09 完成，v1.3.0）

| 项 | 计划 | 实际 |
| --- | --- | --- |
| 内联样式清债 | 60 处静态全收编 | ✅ app.js 60 处 + index.html 4 处全部收编；剩余 5 处全是自定义属性（`--w` / `--p`） |
| 例外 | 未预见 | index.html 里 9 处由 JS 用 `el.style.display` 控制显隐的元素**保留内联**，改 class 会残留 none 导致元素再也不显示（已在文件内注释说明 + selftest 断言豁免） |
| 令牌层 | 补尺度令牌 | ✅ 新增 `--sp-* / --r-* / --fs-* / --lh-*`（取值对齐现有魔法数），另写审计注释记录三层结构与「浅色变量重复定义、下方块才生效」的陷阱 |
| CSS 分段 | 6 段注释 | ✅ 令牌 / 基础 / 外壳 / 面板 / 状态与动效 / 响应式 + 新增第 7 段工具类 |
| 视觉零变化 | 截图对比 | ✅ 额外做了**等价性脚本校验**：把备份里的每条内联声明与替代类的解令牌后声明做集合比对，34 条全部等价（3 条例外已人工确认依据） |
| 防回潮 | selftest 断言 | ✅ 新增 3 条（内联形态 / index.html 例外 / u-* 类必有定义），回归 363 → **367/367** |

**U2 起手提示**：utilities 在第 7 段且位于文件末尾（为复刻内联优先级）。U2 全面重设计时，
这一整段要么并入组件规则、要么整体重画，**不要**在它之后再追加规则——会破坏收编时定下的优先级约定。

---

## 0. 决策记录（2026-09-09 用户拍板）

| 决策 | 结论 | 影响 |
| --- | --- | --- |
| D1 视觉方向 | **C. 全面重设计**——彻底重做一套视觉语言，两主题同时换血 | U2 从「调值」升级为「设计定稿 + 全量换血」；见 §3-U2 修订 |
| D2 内联样式 | **静态的全部收编**——60 处逐个改成 class + token，一次清零 | U1 改动面到顶（~60 处模板 + styles.css），靠 selftest 断言 + 截图巡检兜底 |
| D3 版本节奏 | **三版渐进**——1.3.0=U1、1.3.1=U2、1.4.0=U3+U4，每版独立回归可叫停 | 因 D1 加重，U2 允许拆成 1.3.1（设计定稿）/ 1.3.2（换血落地）两版，见 §3 |

---

## 1. 现状盘点（摸过代码的结论，不是感觉）

规模：`styles.css` 1286 行 / `app.js` 5744 行（其中大量是生成 HTML 的模板字符串）/ 43 个 CSS 设计令牌 / 17 个 @keyframes / 响应式三档（900 / 720 / 480）。

### 好底子（别推翻，站在上面改）

1. **令牌体系完整**：深浅双主题全靠换 `--var` 实现，还做了组件别名层（`--card/--border/--text`）。这是 UI 进化最值钱的资产。
2. **对比度已达标**：`--muted` 曾专门从 4.23:1 修到 AA 达标（注释里留着过程），说明可访问性有基础。
3. **动效有降级**：`prefers-reduced-motion` 三处降级，动画不是裸奔。
4. **i18n 全覆盖**：`data-i18n` 贯穿。
5. **交互细节做过**：modal 有焦点陷阱（FOCUSABLE_SEL + Esc），chip 组实现了 radiogroup/radio + aria-checked + 焦点轮换。

### 真实问题（按疼痛程度排序）

| # | 问题 | 证据 |
| --- | --- | --- |
| P1 | **app.js 模板里 60 处内联样式**。`width:%`、`display:none`、`color:var(--muted)` 散在生成字符串里，改一个视觉要全文搜；个别地方硬编码不走 token | `grep -o 'style="' app.js \| wc -l` = 60 |
| P2 | **双主题气质分裂**。light 是理性专业风（蓝/石板/白卡片），dark 是赛博朋克霓虹（品红/青 glow + Orbitron 标题字体）。切个主题像换了个软件 | 两套 token 块对比 |
| P3 | **显示字体从未加载**。Orbitron/Rajdhani 直接 `font-family` 引用，但全项目没有 `@font-face` 也没有 webfont link——装了这两款字体的机器是赛博朋克，没装的是普通 sans，视觉不可控且离线优先原则下不能指望 CDN | grep 无任何加载来源 |
| P4 | **「侧栏+列表」组件重复 4 套以上**。kb-side/dom-nav、range-side/cat-list×3、tools-side、quiz-side 各一套类名各一套样式 | index.html 结构统计 |
| P5 | **面板布局与状态不统一**。6 个面板 5 个左右分栏、news 单列；面板头部有的带 toolbar 有的没有；空态/loading 文案散落各处 | 逐面板走查 |
| P6 | **语义与可达性缺口**。rail 导航是 `<div>` 不是 `<nav>`+button；`:focus-visible` 全文件仅 3 处；设置等 modal 是字符串拼 HTML | grep 统计 |

现有安全网：前端 selftest 363 断言 + 更新 UI 专项。UI 改造的回归就靠它 + 截图走查。

---

## 2. 决策项（✅ 已于 2026-09-09 全部拍板，保留原文供追溯）

### D1 视觉方向 → **选定 C. 全面重设计**

| 选项 | 内容 | 代价 |
| --- | --- | --- |
| A. 霓虹降调（原推荐） | 保留赛博朋克基因（霓虹 accent、glow、Orbitron 标题），但降饱和、收敛使用范围。dark 依旧赛博，light 做成"纸上的霓虹"——同一骨架，只换 token 值 | 实现路径最短：U1 收口 token 后，U2 基本只调值 |
| B. 全面专业风 | 两主题都是现在 light 的气质，dark 只是深色版。最像"成熟软件"，丢个性 | 推翻 dark 的霓虹资产，改的量反而大 |
| C. 全面赛博（原案内选项） | light 也霓虹化，个性最强 | 学习工具要长时间读正文（知识点/题解/对话），浅底霓虹累眼 |
| **C+. 全面重设计（✅ 选定）** | 彻底重做一套视觉语言，两主题同时换血：新色板、新字体层级、新形状语言、新动效基调 | 工作量最大；U2 必须先出「设计定稿」再动手，且老用户观感变化最大，发布说明要带前后对比图 |

### D2 内联样式清债范围 → **选定：静态的全部收编**

60 处里约 50 处是静态样式（该进 CSS 类），约 10 处是 JS 动态计算的值（如进度条 `width:${mastPct}%`——值必须 JS 算，但样式外壳收敛成统一 `.bar-fill` 组件类）。**✅ 按用户选择执行全量收编：静态 50 处逐个改 class + token，动态 10 处收口到统一 helper/组件类**，一次清零不留特例。改动面最大但只需做一次，selftest 加「内联样式计数 ≤ N」断言防回潮。

### D3 节奏 → **选定：三版渐进**

- **✅ 1.3.0 = U1 清债、1.3.1 = U2（因 D1=C 允许拆为 1.3.1 设计定稿 + 1.3.2 换血落地）、1.4.0 = U3+U4**。每版独立回归、可随时叫停，符合"每版必出安装包"红线。
- 单分支做完再合：风险集中，regression 一次爆雷难定位，不推荐（已否）。

---

## 3. 分阶段计划

### U1 清债（目标：一行视觉效果都不变）

| 动作 | 说明 |
| --- | --- |
| token 审计 | 43 个变量理成「核心令牌 + 别名 + 待淘汰」三层；补间距/圆角/字号层级 token（现在间距全是魔法数） |
| 内联样式清债 | 按 D2 执行：静态 50 处进组件类/utility 类，动态 10 处收口到 `.bar-fill` 等 |
| 字体挂起决策 | Orbitron/Rajdhani 被引用但从未加载。U1 阶段**不改字体行为**（保住"一行视觉不变"），只在代码处注释标记 TODO；去留随 U2a 设计定稿一并决策（保留则 woff2 打包，弃用则换系统栈） |
| CSS 分段 | 1286 行按 tokens / base / components / panels / responsive 分注释段，只加注释不挪规则（挪规则留 U3） |

**验收**：双主题 × 8 面板截图对比，肉眼无差异；363 自测全绿；selftest 加一条「内联样式计数 ≤ N」断言防回潮。

**改动面**：styles.css、app.js 模板 ~60 处、index.html 少量。风险最低，先做。

### U2 全面重设计（按 D1=C 修订：先定稿、再换血）

> 原 U2 是「双主题同骨架调值」。D1 选定全面重设计后，U2 升级为两步：**U2a 设计定稿（不动代码，出规范）→ U2b 全量换血（按规范落地）**。若一版装不下，按 D3 拆为 1.3.1 / 1.3.2 两版。

**U2a 设计定稿（产出 `DESIGN-SPEC.md`，先给用户过目再动手）**

| 动作 | 说明 |
| --- | --- |
| 色板重做 | 两主题各一套完整色板：底色层级（≥3 层）、语义色（brand/accent/warn/ok/bad）、正文层级；对比度全部按 ≥ AA 设计（先在色板上达标，再落地） |
| 字体层级重定 | display / heading / body / mono 四级；**是否保留 Orbitron 系显示字体作为新视觉语言的一部分，在定稿时一并决策**（保留则 woff2 子集化打包，约 50–100KB/款，OFL 许可需在 LICENSE 注明） |
| 形状与密度 | 圆角档位、间距 4px 栅格、阴影/发光三档、控件尺寸（按钮/输入/卡片高度）统一规格 |
| 动效基调 | 17 个 @keyframes 梳理为「保留/简化/删除」三清单；统一时长与缓动曲线；reduced-motion 降级策略沿用 |
| 设计样张 | 用 2 个代表面板（如知识库 + 测验）出双主题静态样张，用户确认后再全量落地 |

**U2b 全量换血（按定稿执行）**

| 动作 | 说明 |
| --- | --- |
| token 值整体替换 | U1 理好的三层令牌结构不动，两主题的值按新色板/新规格全量替换——这是换血的主要实现路径，只改 token 层 + 少量组件样式 |
| 组件样式对齐新规格 | 按钮/输入/chip/modal/卡片逐类过一遍，去掉游离于令牌外的硬编码值 |
| @keyframes 按三清单执行 | 删除/简化的动画同步清理调用点 |
| 面板头部统一 | 6 个面板统一 header 模式（标题 + toolbar + 计数），news 补齐 |
| 全面板走查换肤 | 8 面板 × 双主题逐个过，游离样式清零 |

**验收（U2a）**：DESIGN-SPEC.md 定稿 + 2 面板样张用户确认；色板对比度数值全达标（spec 上标注实测值）。
**验收（U2b）**：对比度全部 ≥ AA；`:root` 之外不存在硬编码颜色（grep 断言）；双主题走查各过一遍；release notes 附新旧对比图。

**改动面**：U2a 零代码；U2b 以 styles.css 为主 + 少量 app.js/index.html。回归风险中——结构不动、只动皮，selftest 全量兜底。

### U3 组件归并（结构性，风险最高，放最后）

| 动作 | 说明 |
| --- | --- |
| 侧栏列表归并 | 4+ 套「侧栏+列表」→ 一个 `.side-list` 组件 + 修饰类 |
| modal 内容模板化 | 设置/报告归档等 modal 从字符串拼接改成模板函数（openModal 已是统一入口，顺势收口） |
| 三态组件 | loading / empty / error 统一组件，替掉散落各处的空态文案 |

**验收**：**删除的 CSS 行数 > 新增的**（归并成功的硬标志）；363 全绿；手工走查 8 面板。

### U4 可达性 + 响应式补全

| 动作 | 说明 |
| --- | --- |
| rail 语义化 | `<div class="rail">` → `<nav>` + `<button>`，保留现有视觉 |
| :focus-visible 全覆盖 | 现在 3 处，补到所有可交互元素 |
| aria 审计 | app.js 生成侧已有 33 处 aria-，但覆盖不全；按 WCAG 常用项补齐（不求全合规） |
| 断点走查 | Electron 主战场是 1280×860，但浏览器直开小窗也要能用；900/720/480 三档各过一遍 |

---

## 4. 安全网与流程

1. **每阶段一个版本走红线流程**：bump → 回归（363 + 56）→ 封包 → Release。UI 版本的 release notes 附双主题截图。
2. **selftest 扩 UI 专项**：内联样式计数断言（防回潮）、`data-i18n` 覆盖抽查。
3. **截图巡检清单**：8 面板 × 双主题 = 16 张必看，断点抽查另计。不追求像素级自动化，人眼巡检 + selftest 兜底。
4. **改一块验一块**：U1 的内联样式清债按面板分批（每次 1–2 个面板），不一次全改。

---

## 5. 明确不做（省得期待落空）

- **不引 UI 框架**（React/Vue/Tailwind）：app.js 5700 行 vanilla 重写等于重做项目，收益为负。
- **不引构建链**：保持纯静态零构建，浏览器直开 `index.html` 必须继续可用。
- **不做移动 App 优先**：主战场是 Electron 桌面 + 浏览器，断点保底即可。
- **不做像素级视觉回归自动化**：工具链引入成本 > 收益，人眼清单够了。

## 6. 风险与已知限制

- U1 清债要动 app.js 60 处模板，回归靠 selftest——selftest 目前不断言视觉，漏改要靠截图巡检兜底。
- 打包字体会让安装包涨 ~100–200KB，且要注意字体 License（Orbitron/Rajdhani 均为 OFL，可自由分发，但 LICENSE 里应注明）。
- 全面重设计（D1=C）后老用户观感变化是四个选项里最大的——U2a 样张先行、用户确认后再动工，发布说明附新旧对比图，把"焕然一新"变成预期而不是惊吓。
- U2a 设计定稿先于 U2b 落地，且样张必须过目：避免换血做到一半方向被推翻，返工成本最高。
- U3 动 app.js 生成逻辑，是全程唯一可能引行为回归的阶段，所以排最后、单独成版。
